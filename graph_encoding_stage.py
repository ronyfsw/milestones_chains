# Encode nodes
# nodes_codes = objects_encoder(Gnodes)
# print(nodes_codes)
# G = nx.relabel_nodes(G, nodes_codes)
# Gnodes, Gedges = list(G.nodes()), G.edges()
# isolates = [nodes_codes[n] for n in isolates]
# terminal_nodes = [nodes_codes[n] for n in terminal_nodes]
# root_node = nodes_codes[root_node]